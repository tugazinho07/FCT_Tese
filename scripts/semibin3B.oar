#!/bin/bash
set -euo pipefail

# Inicializar conda (robusto em batch)
eval "$(/home/ga.soares/ENTER/condabin/conda shell.bash hook)"
conda activate SemiBin

# Opcional: ir para uma pasta de trabalho
cd /home/ga.soares

# Criar output dir (boa prática)


SemiBin2 single_easy_bin \
  -i /home/ga.soares/MegaHit/megahit_result_3B/final.contigs.fa \
  -b /home/ga.soares/alignment_sorted3B.bam \
  -o output-3B
  --environment global
